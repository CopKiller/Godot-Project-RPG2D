using Godot; using System.Text.RegularExpressions;   public partial class MainMenu : Control {     public override void _Ready()     {         ShowMainMenu();     }      // Called every frame. 'delta' is the elapsed time since the previous frame.     public override void _Process(double delta)     {      }      private void ShowMainMenu()     {         GetNode<ColorRect>("MainContainer").Visible = true;     } } 